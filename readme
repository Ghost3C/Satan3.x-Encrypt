编程环境：
使用VS2008编写，MFC程序，多字节编码，不知道为什么unicode编码不行，有些函数不支持吗？

说明：
加密方法是用winAPI，RC4算法
初始版本为了解决V3.1（V4）加密版本，一些地方比如HardwareID、PUBLIC位置和结尾截取冗余数据写死了
第二个版本为了编写通用版本，目前可以解密V3和V3.1（V4），通过搜索几个常见格式，第一次解密需要检测，比较文件前面两位格式解密是否正确
